# 42.20.1.8 GitHub 跨设备实测交接

本文件用于另一台设备只通过 GitHub 获取本轮全部内容。不要依赖本机路径、Workshop 快照或聊天附件。

## 仓库身份

- GitHub：`https://github.com/sj18627803120-source/GodSystem`
- 分支：`fix/42.20.1.8-keybind-task-sort`
- 基线：`42.20.1.7@0924542f72d788f987ef020d785f2835a896971d`
- 首个模块提交：`3b0948cc1157f6d7d69078f2cec3d35001e50b14`
- 功能接入提交：`0e1d615683ae80ae86099d1c93f2899460ca8d64`
- 最终测试提交：以该远端分支最新提交为准，并与交付消息中的完整 SHA 核对。
- Mod ID：`GodSystem_CN`
- Workshop ID：`3773949382`

## 获取方式

```powershell
git clone https://github.com/sj18627803120-source/GodSystem.git
cd GodSystem
git fetch origin
git checkout fix/42.20.1.8-keybind-task-sort
git pull --ff-only origin fix/42.20.1.8-keybind-task-sort
git status --short --branch
```

如果已有仓库，必须在独立工作树或清洁目录中切换；不要在 Workshop 运行目录直接开发。

## 阅读顺序

1. `AGENTS.md`
2. `docs/GodSystem_DevHandoff_CN/00_继续开发入口.md`
3. `docs/GodSystem_DevHandoff_CN/80_42.20.1.7_正式发布与继续开发交接.md`
4. `docs/GodSystem_DevHandoff_CN/81_42.20.1.8_快捷键与任务显示排序.md`
5. 本文件

## 验证命令

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\Test-GodSystem.ps1
git diff --check
git status --short --branch
```

通过后，仅把当前分支的 `Contents`、`workshop.txt` 和 `preview.png` 同步到隔离的 Workshop 测试目录。不要覆盖已发布稳定备份。

## 实机重点

- 默认 N 开关面板；设置页改键、Esc 取消、恢复 N、退出重进保持。
- 聊天、搜索框、确认弹窗期间不误触。
- “快捷”按钮仍然是原快捷操作窗，“设置”是主窗口中的薄页面。
- 待接、进行中和任务追踪器按分类、D1→D4、标题一致排序。
- 刷新任务、接取任务和 MP 同步后不恢复为服务器数组的原顺序。
- 任务接取、领取、放弃、经济、仓库、终端、装填机和机器人保持 42.20.1.7 行为。

发现问题时回传：完整红字、复现步骤、新档/旧档、SP/MP、当前分支和 `git rev-parse HEAD`。自动测试通过不能替代 B42.20 实机结论。
