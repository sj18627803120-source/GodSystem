# v1.16.35 医疗服务、快捷窗与导航修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `C:\Users\Admin\Zomboid\Workshop\GodSystem` 的实时测试目录中实现 v1.16.35：升级页医疗服务、基础快捷悬浮窗、左侧导航越界修复。

**Architecture:** 单人路径继续在 `GodSystem_Core.lua` 本地执行，保留现有稳定数据流；多人路径新增 `medicalService` 协议和服务端 `Commands.medicalService`，由服务端扣费和修改角色身体状态，客户端只负责确认、pending、反馈和成功后的幂等兜底。UI 改动保持在现有 `GodSystem_UI.lua` 固定面板体系内，不新建左侧大页签。

**Tech Stack:** Project Zomboid Build 42.19 Lua、GodSystem SP/MP 单 MOD 条目、PowerShell 静态测试脚本、现有编码检查脚本。

## Global Constraints

- 目标游戏版本：Project Zomboid B42.19。
- 实时目录：`C:\Users\Admin\Zomboid\Workshop\GodSystem`。
- 当前基线：`v1.16.34`。
- 实现后版本：`v1.16.35`。
- 实现前必须备份当前 `v1.16.34`。
- 本次不覆盖用户确认过的稳定发布包 `GodSystem_v1.15.3_B42_WorkshopUpload.zip`。
- 版本升级后必须运行滚动备份，备份目录只保留最新 20 个版本。
- 新增中文必须同步 `GodSystem_Localization_Override.lua`、`Translate\CN\IG_UI_CN.txt`、`Translate\CH\IG_UI_CH.txt`。
- Lua fallback 新增中文优先使用 UTF-8 十进制字节转义，避免游戏内乱码。

---

### Task 1: 测试脚本和备份

**Files:**
- Create: `C:\Users\Admin\Desktop\PJ\Test-GodSystemV11635.ps1`
- Read: `C:\Users\Admin\Desktop\PJ\Backup-GodSystem.ps1`

**Interfaces:**
- Produces: `Test-GodSystemV11635.ps1` 静态检查脚本。

- [ ] **Step 1: 运行 v1.16.34 备份**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File 'C:\Users\Admin\Desktop\PJ\Backup-GodSystem.ps1' -Source 'C:\Users\Admin\Zomboid\Workshop\GodSystem'
```

Expected: 在 `C:\Users\Admin\Desktop\PJ\GodSystem_Backups` 生成 v1.16.34 滚动备份或确认脚本按现有规则完成。

- [ ] **Step 2: 写失败测试**

Create `Test-GodSystemV11635.ps1` to check:

```powershell
$Root = 'C:\Users\Admin\Zomboid\Workshop\GodSystem'
$errors = New-Object System.Collections.Generic.List[string]
function Read-Utf8([string]$Path) { [System.IO.File]::ReadAllText($Path, [System.Text.Encoding]::UTF8) }
function Require-Text([string]$Path, [string]$Pattern, [string]$Message) {
    $text = Read-Utf8 $Path
    if ($text -notmatch $Pattern) { $script:errors.Add($Message) }
}
$mod = Join-Path $Root 'Contents\mods\GodSystem'
$config = Join-Path $mod '42\media\lua\shared\GodSystem_Config.lua'
$admin = Join-Path $mod '42\media\lua\shared\GodSystem_AdminConfig.lua'
$protocol = Join-Path $mod '42\media\lua\shared\GodSystem_Protocol.lua'
$core = Join-Path $mod '42\media\lua\client\GodSystem_Core.lua'
$network = Join-Path $mod '42\media\lua\client\GodSystem_Network.lua'
$ui = Join-Path $mod '42\media\lua\client\GodSystem_UI.lua'
$server = Join-Path $mod '42\media\lua\server\GodSystem_Server.lua'
$override = Join-Path $mod '42\media\lua\shared\GodSystem_Localization_Override.lua'
$cn = Join-Path $mod '42\media\lua\shared\Translate\CN\IG_UI_CN.txt'
$ch = Join-Path $mod '42\media\lua\shared\Translate\CH\IG_UI_CH.txt'
Require-Text $config 'GodSystemConfig\.Version\s*=\s*"1\.16\.35"' 'Config version is not 1.16.35'
Require-Text (Join-Path $mod 'mod.info') 'modversion=1\.16\.35' 'root mod.info version is not 1.16.35'
Require-Text (Join-Path $mod '42\mod.info') 'modversion=1\.16\.35' '42 mod.info version is not 1.16.35'
foreach($name in 'MedicalCheckInfectionCost','MedicalSuppressInfectionCost','MedicalSuppressInfectionHours','MedicalHealInjuriesCost','MedicalCureInfectionCost'){
    Require-Text $config "GodSystemConfig\.$name" "Missing config $name"
    Require-Text $admin $name "Missing admin config $name"
}
Require-Text $protocol 'MedicalService\s*=\s*"medicalService"' 'Missing protocol medicalService'
Require-Text $protocol 'medicalService\s*=\s*true' 'medicalService missing from command sets'
Require-Text $core 'function\s+GodSystem\.performMedicalService' 'Missing SP medical service'
Require-Text $core 'function\s+GodSystem\.applyMedicalServiceLocally' 'Missing local medical application helper'
Require-Text $network 'performMedicalService' 'Missing MP medical wrapper'
Require-Text $server 'function\s+Commands\.medicalService' 'Missing server medicalService handler'
Require-Text $ui 'confirmMedicalService' 'Missing medical confirmation UI'
Require-Text $ui 'GodSystemShortcutWindow' 'Missing shortcut window'
Require-Text $ui 'y\s*>=\s*top\s*and\s*\(y\s*\+\s*h\)\s*<=\s*bottom' 'Nav visibility still allows partial rows'
foreach($key in 'Upgrade_Medical_CheckInfection','Upgrade_Medical_SuppressInfection','Upgrade_Medical_HealInjuries','Upgrade_Medical_CureInfection','Btn_Shortcuts','Shortcut_Title','NotifyMP_MedicalService'){
    Require-Text $override $key "Override missing $key"
    Require-Text $cn $key "CN missing $key"
    Require-Text $ch $key "CH missing $key"
}
if($errors.Count -gt 0){ $errors | ForEach-Object { Write-Error $_ }; exit 1 }
Write-Output 'Test-GodSystemV11635 passed'
```

- [ ] **Step 3: 运行测试确认失败**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File 'C:\Users\Admin\Desktop\PJ\Test-GodSystemV11635.ps1'
```

Expected: FAIL because version and new symbols are missing in v1.16.34.

### Task 2: 配置、协议和本地化

**Files:**
- Modify: `GodSystem_Config.lua`
- Modify: `GodSystem_AdminConfig.lua`
- Modify: `GodSystem_Protocol.lua`
- Modify: `GodSystem_Localization_Override.lua`
- Modify: `Translate\CN\IG_UI_CN.txt`
- Modify: `Translate\CH\IG_UI_CH.txt`

**Interfaces:**
- Produces config keys: `MedicalCheckInfectionCost`, `MedicalSuppressInfectionCost`, `MedicalSuppressInfectionHours`, `MedicalHealInjuriesCost`, `MedicalCureInfectionCost`.
- Produces command: `medicalService`.

- [ ] **Step 1: Add config/admin metadata**
- [ ] **Step 2: Add protocol command to C2S, StateCommands and KeyCommands**
- [ ] **Step 3: Add CN/CH/Override localization keys**
- [ ] **Step 4: Run `Test-GodSystemV11635.ps1` and expect remaining failures outside this task**

### Task 3: 医疗服务 SP/MP 逻辑

**Files:**
- Modify: `GodSystem_Core.lua`
- Modify: `GodSystem_Network.lua`
- Modify: `GodSystem_Server.lua`

**Interfaces:**
- Produces: `GodSystem.getMedicalStatus(player)`.
- Produces: `GodSystem.applyMedicalServiceLocally(action, player)`.
- Produces: `GodSystem.performMedicalService(action)`.
- Produces: `GodSystemNetwork.performMedicalService(action)`.
- Produces: `Commands.medicalService(_, _, player, args)`.

- [ ] **Step 1: Add SP helpers with `pcall`-guarded B42 body methods**
- [ ] **Step 2: Add MP wrapper**
- [ ] **Step 3: Add server handler with action whitelist, affordability checks, structured MP notify/history, and result payload**
- [ ] **Step 4: Ensure MP client applies local idempotent fallback only after success result**
- [ ] **Step 5: Run `Test-GodSystemV11635.ps1` and expect only UI/version/docs failures if any**

### Task 4: UI 医疗服务、快捷窗、导航修复

**Files:**
- Modify: `GodSystem_UI.lua`

**Interfaces:**
- Produces: `GodSystemWindow:confirmMedicalService(service)`.
- Produces: `GodSystemWindow:onMedicalServiceConfirm(button, payload)`.
- Produces: `GodSystemShortcutWindow`.
- Produces: full-row-only nav visibility.

- [ ] **Step 1: Add mixed upgrade/medical rows and details**
- [ ] **Step 2: Route primary button for `medicalService` payload to confirmation modal**
- [ ] **Step 3: Add top tool button `快捷` and fixed shortcut floating window**
- [ ] **Step 4: Fix nav visibility to show only full rows and step-scroll max**
- [ ] **Step 5: Run `Test-GodSystemV11635.ps1` and expect version/docs failures if any**

### Task 5: 版本、记录、验证和备份

**Files:**
- Modify: `GodSystem_Config.lua`
- Modify: `Contents\mods\GodSystem\mod.info`
- Modify: `Contents\mods\GodSystem\42\mod.info`
- Modify: `workshop.txt`
- Modify: `README_创意工坊上传说明.md`
- Modify: `GodSystem_DevHandoff_CN\07_版本记录.md`

**Interfaces:**
- Produces final version `1.16.35`.

- [ ] **Step 1: Bump versions to `1.16.35`**
- [ ] **Step 2: Add v1.16.35 version record**
- [ ] **Step 3: Run v1.16.35 static test**
- [ ] **Step 4: Run encoding check**
- [ ] **Step 5: Run command parity checks**
- [ ] **Step 6: Run Lua parse checks if `luac` is available**
- [ ] **Step 7: Generate rolling v1.16.35 backup**
