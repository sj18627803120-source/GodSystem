# v1.16.15 UI 重制实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` or `superpowers:executing-plans` to implement this plan task-by-task. Follow `pz-mod-dev` first.

**目标：** 废弃失败的 v1.16.14 UI 实现，基于 v1.16.13 稳定代码重制一个保守、稳定、暗金风格的 v1.16.15 UI。

**架构：** 先恢复实时目录到 v1.16.13，再只在 v1.16.13 行为模型上做外观和布局收敛。保留 v1.16.13 的数据流、多人轻同步、任务双列、银行五动作、记录/说明/诊断单列和任务追踪窗口；不复用 v1.16.14 的大面积 render 覆盖。

**技术栈：** Project Zomboid B42.19 Lua / ISUI / Workshop 目录结构 / GodSystem 现有 SP/MP 桥。

## 全局约束

- 实时目录：`C:\Users\Admin\Zomboid\Workshop\GodSystem`。
- 基线备份：`C:\Users\Admin\Desktop\PJ\GodSystem_Backups\GodSystem_v1.16.13_WorkshopBackup_20260617_140501.zip`。
- v1.16.14 视为失败版本，只保留设计教训，不在其实现上继续修补。
- 新版本号为 `1.16.15`。
- 功能改动后必须同步 `GodSystemConfig.Version`、两个 `mod.info`、`workshop.txt`、上传说明和版本记录。
- 每次版本升级必须运行 `Backup-GodSystem.ps1`，保留最新 20 个版本。
- 所有中文/本地化改动后必须运行 `Test-GodSystemEncoding.ps1`。
- UI render/prerender 只做绘制，不做网络请求、任务生成、列表重建或数据写入。
- 禁止使用 `drawText(..., gsTextColor(...), UIFont.Small)` 这类中间多返回值写法；文字绘制必须显式传入 `r,g,b,a,font` 或走安全 wrapper。
- 纹理和图标只能是装饰，加载失败不得影响文字和布局。
- 不全局 monkey patch 原版 `ISModalDialog` 或其他 ISUI 类。

---

## Task 1: 备份失败版并恢复 v1.16.13 基线

**Files:**
- Replace live tree under `C:\Users\Admin\Zomboid\Workshop\GodSystem`
- Source zip: `C:\Users\Admin\Desktop\PJ\GodSystem_Backups\GodSystem_v1.16.13_WorkshopBackup_20260617_140501.zip`

**Steps:**
- [x] Run rolling backup for current live v1.16.14 before replacing it.
- [x] Expand v1.16.13 backup to a temp folder.
- [x] Verify expanded root contains `Contents`, `workshop.txt`, `preview.png`.
- [x] Replace the live Workshop root with the expanded v1.16.13 content.
- [x] Verify live config reports version `1.16.13` before starting new edits.

## Task 2: Add conservative UI theme layer

**Files:**
- Create: `C:\Users\Admin\Zomboid\Workshop\GodSystem\Contents\mods\GodSystem\42\media\lua\client\GodSystem_UITheme.lua`
- Modify: `C:\Users\Admin\Zomboid\Workshop\GodSystem\Contents\mods\GodSystem\42\media\lua\client\GodSystem_UI.lua`

**Steps:**
- [x] Add a small `GodSystemUITheme` table with colors, minimum size, default size, row/button spacing, and optional icon paths.
- [x] Add safe color helpers in `GodSystem_UI.lua` that unpack theme colors into explicit draw arguments.
- [x] Do not draw text from theme helpers until wrappers are in place.
- [x] Keep existing v1.16.13 window behavior and layout metrics as the source of truth.

## Task 3: Apply stable visual shell

**Files:**
- Modify: `GodSystem_UI.lua`

**Steps:**
- [x] Restyle the main window background, borders, top status area, list/detail panels, and action bar using simple rectangles and borders.
- [x] Keep existing `setupLayoutMetrics()`, `applyBaseLayout()`, `setActionBar()`, `populate*()` data flow intact.
- [x] Use original ISUI button labels first. Do not override every button render in this task.
- [x] Keep record/info/diagnostics single-column layout unchanged.
- [x] Keep task dual-column layout and `selectedTaskList` restore behavior unchanged.

## Task 4: Add optional icons safely

**Files:**
- Modify: `GodSystem_UI.lua`
- Optional existing assets only: `media/ui/GodSystem/*`

**Steps:**
- [x] v1.16.13 restore后没有保留 v1.16.14 图标资产；本轮跳过可选图标渲染。
- [x] 保持导航文字按钮作为主路径，缺失图标不会影响文字、尺寸或点击行为。
- [x] 后续若重新加入图标，必须通过安全 texture getter 且只作为装饰。

## Task 5: Version, docs, and validation

**Files:**
- Modify: `GodSystem_Config.lua`
- Modify: both `mod.info`
- Modify: `workshop.txt`
- Modify: upload README if present
- Modify: `GodSystem_DevHandoff_CN\00_继续开发入口.md`
- Modify: `GodSystem_DevHandoff_CN\02_功能现状.md`
- Modify: `GodSystem_DevHandoff_CN\03_开发经验与踩坑.md`
- Modify: `GodSystem_DevHandoff_CN\05_测试清单.md`
- Modify: `GodSystem_DevHandoff_CN\07_版本记录.md`

**Steps:**
- [x] Bump version to `1.16.15`.
- [x] Document v1.16.14 failure root cause and mark it abandoned.
- [x] Document v1.16.15 as based on v1.16.13 with conservative UI shell.
- [x] Run encoding check on live MOD and handoff docs.
- [x] Run command parity and obvious static checks.
- [x] Create rolling backup for v1.16.15.
