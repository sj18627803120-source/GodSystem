# v1.16.36 快捷动作栏与左侧翻页实施计划
> 历史实施计划：按根目录 `AGENTS.md` 和任务复选框逐项执行；当前流程不依赖 Superpowers 技能。

**Goal:** 在 `C:\Users\Admin\Zomboid\Workshop\GodSystem` 中把快捷窗改为四个直接动作，并把左侧导航改为显式上下翻页。

**Architecture:** 快捷栏继续使用当前 `GodSystemShortcutWindow`，但用动作列表替换页面模式列表。传送、腰包出售、银行存款都复用现有业务入口，仅为“全部现金存入活期”补一个 `depositAllCash` 银行动作。左侧导航保留现有按钮池，新增翻页状态和上下翻页按钮，中间区域只布局当前页完整按钮。

**Tech Stack:** Project Zomboid Build 42 Lua、GodSystem SP/MP 单 MOD、PowerShell 静态测试、现有编码检查与滚动备份脚本。

## Global Constraints

- 实时测试目录：`C:\Users\Admin\Zomboid\Workshop\GodSystem`
- 基线版本：`v1.16.35`
- 目标版本：`v1.16.36`
- 不替换稳定包 `GodSystem_v1.15.3_B42_WorkshopUpload.zip`
- 版本升级后必须运行滚动备份，保留最新 20 个版本
- 中文本地化同步 `GodSystem_Localization_Override.lua`、`Translate\CN\IG_UI_CN.txt`、`Translate\CH\IG_UI_CH.txt`
- 多人模式下实体货币移除和活期写入必须由服务端执行

---

### Task 1: 失败测试

**Files:**
- Create: `C:\Users\Admin\Desktop\PJ\Test-GodSystemV11636.ps1`

**Interfaces:**
- Produces: v1.16.36 快捷栏、银行、导航分页、版本和本地化静态检查。

- [ ] **Step 1: 写入失败测试脚本**
- [ ] **Step 2: 运行脚本，确认当前 v1.16.35 失败**

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File 'C:\Users\Admin\Desktop\PJ\Test-GodSystemV11636.ps1'
```

Expected: FAIL because v1.16.35 still has page-mode shortcuts and no `depositAllCash` action.

### Task 2: 银行全部现金存入活期

**Files:**
- Modify: `GodSystem_Core.lua`
- Modify: `GodSystem_Server.lua`

**Interfaces:**
- Produces: `GodSystem.depositAllCashToBankCurrent()`
- Extends: `GodSystem.performBankAction("depositAllCash")`
- Extends: server `Commands.bank` action `depositAllCash`

- [ ] **Step 1: 单人实现扫描当前背包系统币总额，金额为 0 时提示无现金**
- [ ] **Step 2: 单人移除全部实体系统币后写入活期，写历史和提示**
- [ ] **Step 3: 多人服务端在 `Commands.bank` 中处理 `depositAllCash`**
- [ ] **Step 4: 服务端用真实背包扫描值作为金额，不读取客户端金额**

### Task 3: 快捷动作栏

**Files:**
- Modify: `GodSystem_UI.lua`

**Interfaces:**
- Produces: `GodSystemShortcutWindow:getActions()`
- Produces: `GodSystemShortcutWindow:performAction(action)`
- Produces: shortcut home confirmation helpers

- [ ] **Step 1: 替换旧页面模式按钮列表**
- [ ] **Step 2: 按家园点和返回点动态显示 `回家`、`返回`**
- [ ] **Step 3: `卖腰包` 调用 `GodSystem.recycleWaistSpaceItems(nil)`**
- [ ] **Step 4: `存现金` 调用 `GodSystem.performBankAction("depositAllCash")`**
- [ ] **Step 5: 传送动作保留确认弹窗**

### Task 4: 左侧导航上下翻页

**Files:**
- Modify: `GodSystem_UI.lua`

**Interfaces:**
- Produces: nav page state and up/down buttons.

- [ ] **Step 1: 新增导航页码状态，计算每页完整按钮数量**
- [ ] **Step 2: 顶部增加上翻按钮，底部增加下翻按钮**
- [ ] **Step 3: 中间区域只布局当前页功能按钮**
- [ ] **Step 4: 保留滚轮作为翻页辅助，不再依赖连续滚动显示半截按钮**

### Task 5: 本地化、版本、验证和备份

**Files:**
- Modify: `GodSystem_Config.lua`
- Modify: `Contents\mods\GodSystem\mod.info`
- Modify: `Contents\mods\GodSystem\42\mod.info`
- Modify: `workshop.txt`
- Modify: `README_创意工坊上传说明.md`
- Modify: `GodSystem_DevHandoff_CN\00_继续开发入口.md`
- Modify: `GodSystem_DevHandoff_CN\07_版本记录.md`

- [ ] **Step 1: 补全快捷栏、银行存入全部现金、导航翻页本地化**
- [ ] **Step 2: 版本升级到 `1.16.36`**
- [ ] **Step 3: 运行 `Test-GodSystemV11636.ps1`**
- [ ] **Step 4: 运行编码检查**
- [ ] **Step 5: 运行 Lua 解析检查**
- [ ] **Step 6: 运行滚动备份脚本**
