# 神级系统 MOD 继续开发入口

当前项目是《Project Zomboid》Build 42.19 测试版 MOD：神级系统 / God System CN。

当前稳定/创意工坊发布版：v1.16.63。

当前 Git 修复分支开发版本：v1.16.68（普通地面控制器与系统仓库运行时热修，尚未通过 B42.19 复测，不是稳定发布版）。

当前实时开发/测试目录：
`C:\Users\Admin\Zomboid\Workshop\GodSystem`

历史稳定备份 zip（未随当前 Git 发布流程更新）：
`C:\Users\Admin\Desktop\PJ\GodSystem_v1.15.3_B42_WorkshopUpload.zip`

滚动版本备份目录：
`C:\Users\Admin\Desktop\PJ\GodSystem_Backups`

后续继续开发前，必须先读本目录，再读当前稳定目录里的主文件：

- `docs\reference-mod-research\README.md`：另一台没有本地参考 MOD 时，先从这里按主题选择证据报告。
- `docs\reference-mod-research\catalog.md`：确认来源版本、证据强度和联网复核关键词。
- `docs\PZ_B42_OFFICIAL_DEVELOPMENT_CN.md`：B42 官方迁移资料、42.19 适用边界和当前接口查证顺序。

- `Contents\mods\GodSystem\42\media\lua\client\GodSystem_Core.lua`
- `Contents\mods\GodSystem\42\media\lua\client\GodSystem_UI.lua`
- `Contents\mods\GodSystem\42\media\lua\shared\GodSystem_Config.lua`
- `Contents\mods\GodSystem\42\media\lua\shared\GodSystem_Localization_Override.lua`
- `Contents\mods\GodSystem\42\media\scripts\GodSystem_Items.txt`

## 固定开发流程

- v1.16.68 是当前修复分支版本：撤销 v1.16.67 的伪 Moveable/街机贴图路线，`GodSystem.StorageController` 恢复为保留自身类型和中文名的普通世界物品，并使用原版“放置物品”落地；修正 SP 直接调用管理器时控制器坐标字段不一致，恢复打开、刷新、索引和收回；补齐系统页“使用说明”详情分支；列表选择恢复改用确定的数字行号，避免 `ISScrollingListBox.prerender()` 的 `__lt` 红字。连接模式不再修改场景对象高亮状态，只在扫描时缓存节点坐标，逐帧绘制绿色已连通/蓝色未连通小符号。新增运行时测试覆盖“地面控制器＋相邻已标记木箱＋箱内已有木板”的完整索引链路。自动化统一回归、编码检查和35个 Lua 5.1 编译已通过，B42.19 SP/MP 放置、右键、收回和真实家具索引仍是发布门槛。详见 `64_v1.16.68_系统仓库运行时热修.md`。
- v1.16.67 是当前修复分支版本：控制器改为原版 `base:moveable` 放置/拾取生命周期，网络身份保存在 `movableData`；删除失败的客户端 `ISBuildingObject` 和 `IsoCarBatteryCharger` 安装路径。连接模式改为给场景家具整体加网络标记，同层四方向紧贴或同格堆叠自动连通，控制器作为桥接节点，每次打开/刷新/存取重新计算最多128个家具节点。系统页首行提供完整“使用说明”，连接模式仅用已连通/未连通两色临时高亮。统一回归、运行时拓扑测试、编码检查和35个 Lua 5.1 编译通过，B42.19 SP/MP Moveable 往返与家具类型实测仍是发布门槛。详见 `63_v1.16.67_物理相邻容器网络实现.md`。
- v1.16.66 是上一失败修复分支版本：根据 B42.19 SP 的 `GodSystem_StorageContext.lua:117` 红字，把安装光标类移入仓库右键模块，但实机研究进一步确认客户端不能直接加载位于 server 目录的 `ISBuildingObject`；该安装路线已由 v1.16.67 完整删除。动作栏的条件显示修复继续保留。实现记录见 `61_v1.16.66_控制器运行时与仓库动作栏修复.md`。
- v1.16.65 是上一修复分支版本：废弃“把普通控制器物品丢到地面后右键”的不稳定入口，改为背包右键安装光标，并复用 B42.19 原版 `IsoCarBatteryCharger` 固定世界对象完成安装、同步和收回。每套网络首次领取免费；重复领取被拦截，遗失或强制找回固定收费2000系统币并使用操作ID防重。成功找回使旧令牌失效，只清理当前玩家背包内能够精确匹配同一网络的旧控制器，不扫描全地图，不触碰仓库连接或真实物品。其独立放置文件在实机未形成预期全局，已由 v1.16.66 替换。实现记录见 `60_v1.16.65_安装式控制器与付费找回修复.md`。
- v1.16.64 是上一功能分支版本：新增与穿戴式空间终端完全独立的系统仓库网络。控制器不是容器，物品始终真实保存在固定场景容器中；SP/MP 共用身份、索引、路由和单件事务管理器，MP 只由服务端移动真实实例。索引按每批最多250件/约2毫秒执行，默认最多20,000件，客户端按类型聚合并在本地组合筛选。其普通地面物品控制器经实机确认无法稳定触发右键，且领取缺少唯一性约束，已由 v1.16.65 替换。实现记录见 `59_v1.16.64_系统仓库网络实现.md`。
- v1.16.63 是当前稳定/创意工坊发布版本：删除 v1.16.62 无效的超49容量方法包装，终端容量恢复为 Lv.1-8/最高49；新增第三条“空间减载”，通过一个隐藏、不可食用、受保护的 Food 实例提供原生负重量。后续同版本修复完整独立穿戴槽、MP 客户端只读、服务端差异同步、按真实 item ID 即时回传终端字段，以及 MOD 车辆部分有效修复后模块不消耗的问题。用户已确认测试通过并发布到创意工坊，后续版本必须把 v1.16.63 作为兼容基线。实现记录见 `56_v1.16.63_空间减载实现.md`、`57_v1.16.63_多人终端穿戴修复.md` 和 `58_v1.16.63_终端即时同步与车辆模块消耗修复.md`。
- v1.16.61 是上一开发/测试版本：统一普通物品 `fullType`、家具 `fullType + worldSprite` 和商城配置 `id` 的商品身份，清理旧存档中的内置重复记录；单件上架改为按物品实例 ID 复验并补齐扣费失败回滚。玩家主动删除解锁商品改为持久隐藏，新增分类/状态/搜索隐藏管理弹窗；隐藏商品不出现在商城列表但仍保留在抽奖候选中，旧 `removeUnlocked` 多人命令兼容为隐藏。实现记录见 `53_v1.16.61_商城身份与隐藏管理实现.md`。
- v1.16.60 是上一实时开发/测试版本：保留 v1.16.59 的负重等级、价格和倍率写入行为，不再把界面写死为每级 `+2` 或 `+3`。升级页只在进入页面、手动刷新或必要状态恢复时读取一次角色负重，显示“实际加成＝最终负重－排除本 Mod 后的基础负重”；SP 购买成功后根据写入后的真实 Java float 倍率测算本次增加量和预计最终负重。测算模块不注册逐帧、定时或库存扫描。终端页新增说明，明确部分特殊重量或其他 Mod 改重物品无法压缩时会被自动跳过。实现记录见 `51_v1.16.60_负重实际测算与压缩说明.md`。
- v1.16.59 是上一实时开发/测试版本，只修复两项实机问题：人物负重升级不再因 `setMaxWeightDelta()` 后立即读取旧的整数最终值而误判失败，基础 14 时一级仍严格为 16；物品压缩把实例 setter 输入与 `getActualWeight()` 的派生有效重量分开保存和校正，覆盖木板等 `HandWeapon`。本版不增加容量绕过、存入菜单或空间仓库。实现记录见 `50_v1.16.59_负重与派生物品压缩修复.md`。
- v1.16.58 是上一实时开发/测试版本：针对实机“减重始终 50%、多物品压缩升级失败、新放入物品不压缩”反馈，终端同时写入并核验外层 `InventoryContainer` 与内层 `ItemContainer` 的容量/减重；压缩先启用实例自定义重量再写真实重量，单件异常只跳过并记录诊断，不再让整批升级回滚。SP 容器变化采用待处理标记和有界签名触发校准，MP 继续由服务端权威低频校准。空间仓库备选未实现。实现记录见 `49_v1.16.58_终端减重与压缩实机反馈修复.md`。
- v1.16.57 是上一实时开发/测试版本：人物负重已修复为把“等级 × 2”换算成原生倍率并核验最终值；空间终端已拆为容量、减重、压缩三条独立升级线，新增实例级原重恢复、服务端权威同步和家具 `fullType + worldSprite` 变体身份。库存右键智能跳过行为继续保留。当前设备的统一静态回归、编码检查、26 个 Lua 的 Lua 5.1 语法解析和三个行为脚本已通过，但正式 Lua 5.1 编译与 B42.19 SP/MP 实机尚未执行。实现记录见 `48_v1.16.57_负重与空间终端三线升级修复.md`。
- v1.16.56 是上一实时开发/测试版本：单人同伴升级为代码内像素线段绘制的机敏战斗无人机；新增 `32×24` 两帧本体、八方向覆盖层、固定 24 个循环粒子、待机/巡航/追赶/警戒/蓄能/发射/恢复/守卫状态，以及真实 0.2 秒蓄能。后续实测修复机器人视觉接入、导航分页填充、跨页动作按钮禁用残留和车辆确认回调；用户已确认 SP 车辆修复模块可正常消耗并修复。实现记录见 `45_v1.16.56_像素机器人与行为升级.md`。
- v1.16.55 是上一开发/测试基线：空间终端改用原版 `base:necklace`；左侧导航按可用高度自适应紧凑分页；悬浮入口改为黑金图标；同伴降低巡航速度并增加非战斗停顿，允许在不可见区域继续移动；灵视范围调整为 20/30/40/50 米，守护冷却调整为 90/60/40/25 秒；同伴与属性操作使用一次性稳定 ID 恢复选择；车辆修复模块显式定价 5000，SP/MP 均通过服务端路径调用共享 `vehicle:repair()`。实现记录见 `44_v1.16.55_实测问题修复.md`。
- v1.16.54 是上一未发布测试版本：修复系统空间终端自定义穿戴位置，终端恢复 Lv.1-8/最大容量49并删除未发布旧腰包迁移；属性页改为“升到下一级”确认；标题、说明和空状态行不再可选；左侧导航均衡分页；守护与过载爆裂补充触发特效。自定义槽未通过实机，已在 v1.16.55 删除。实现记录见 `43_v1.16.54_空间终端与UI修正.md`。
- v1.16.53 是未发布测试版本：新增属性经验、车辆修复模块和同伴五项攻击特效，但终端穿戴定义与 Lv.9-12 容量设计存在问题，不作为兼容基线。
- v1.16.52 是旧机器人视觉基线：机器人改为用 `renderline()` 直接绘制的蓝色双层菱形，删除 16 张 PNG 和 B42.19 无可用重载的 `SpriteRenderer.render(Texture,...)`；v1.16.56 在这条安全路径上升级为编译后的像素横线段，不恢复机器人 PNG。实现记录见 `41_v1.16.50_同伴安全与表现修复设计.md`。
- v1.16.47 新增系统修复组件和耐久强化核心，支持标准 B42 耐久接口并排除出抽奖奖池。实现记录见 `38_v1.16.47_修复组件与耐久强化核心.md`。
- v1.16.46 是自动银行、自动任务与风险理财基线：在 v1.16.45 基础上新增每1游戏小时自动交任务、自动把随身现金存入活期，以及稳健/平衡/激进三个按在线24游戏小时结算的理财账户；停止在 UI 创建新死期，旧存单只保留支取。实现记录见 `37_v1.16.46_自动银行自动任务与风险理财.md`。
- v1.16.45 是上一测试版本：新增腰包独立回收模式，腰包页可在“仅回收”和“回收上架”之间切换；手动选中、手动全部、快捷栏卖腰包、自动腰包回收都跟随该腰包模式。多人本轮触及提示改为服务端发 `code/args`、客户端本地化显示；新增 YAML 本地化源和生成器，Lua fallback 使用 ASCII-only UTF-8 字节转义。实现记录见 `36_v1.16.45_腰包回收模式与中文治理.md`。
- v1.16.44 是银行借贷本地化修复基线：在 v1.16.43 银行借贷系统基础上修复借贷 UI 本地化乱码。借贷相关 `Translate\CN/CH\IG_UI_*.txt` 表项必须带行尾逗号；`GodSystem_Localization_Override.lua` 中新增中文 fallback 继续使用 UTF-8 十进制字节转义。
- v1.16.42 是上一轮医疗修复测试版本：在 v1.16.41 基础上参考 RaccoonCityB42 生化疫苗逻辑，治愈病毒会同时清 `CharacterStat.ZOMBIE_INFECTION`，避免 BodyDamage 进度清空但真实僵尸病毒统计残留。
- v1.16.41：在 v1.16.40 基础上紧急废弃并移除“压制病毒”，避免感染时间写入触发瞬死；医疗服务只保留检查感染、治疗伤势、治愈病毒。
- v1.16.40 是上一版测试版本：在 v1.16.39 基础上修正医疗服务；检查感染不改，压制病毒 200 只清当前感染进度，治愈病毒 2000 清 BodyDamage 与 body part 两层感染状态。注意：压制病毒已在 v1.16.41 废弃。
- v1.16.39 是上一版测试版本：在 v1.16.38 基础上新增回收页右键“仅上架”，只解锁商城购买项，不删除物品；多人由服务端验证真实物品和余额。
- v1.16.36 是快捷动作栏与左侧翻页基线：快捷悬浮窗改为回家、返回、卖腰包、存现金四个直接动作，左侧导航改为显式上翻/下翻分页。
- v1.16.35 是上一版测试基线：基于 v1.16.34，升级页新增医疗服务，标题栏新增基础快捷悬浮窗，左侧导航滚动改为只显示完整按钮。
- v1.16.34 是上一版待测基线：抽奖奖池新增 shared 物品可用性过滤层，排除隐藏、废弃、伤口、僵尸伤害、身体绷带、测试杯和占位物品。
1. 本项目不再使用 Superpowers 技能。PZ MOD 工作直接遵循根目录 `AGENTS.md`、仓库内 `pz-mod-dev`、本交接目录和当前任务计划；需求不清先确认，Bug 先从日志和调用链定位根因，多文件任务先列最小步骤，交付前用实际命令验证。
2. 先确认用户要改的是当前实时测试目录还是历史 zip。当前稳定/创意工坊发布版是 v1.16.63；v1.15.3 zip 只保留为历史备份。
3. 先读本交接目录，特别是 `03_开发经验与踩坑.md`、`08_参考MOD研究笔记.md`。
4. 做任何新功能前，先确认 Project Zomboid 版本。当前目标是 B42.19。
5. 参考优先级按“版本一致性 + 官方性”决定：B42.19 当前原版 Lua/脚本/反编译 Java最高；明确适用于当前系统的官方迁移资料第二；同版本参考 MOD 第三；旧版本资料只能作为次级参考。
6. The Indie Stone 当前公开的系统性 MOD 开发资料主要是 B42.13 迁移指南和 Inventory Items API，并没有单独的 42.19 MOD 手册。采用其中规则前仍需核对 B42.19 原版实现；详见 `docs/PZ_B42_OFFICIAL_DEVELOPMENT_CN.md`。
7. 不再默认把用户提供的参考 MOD 当作同版本。必须按报告标注为 `B42.19 同版本证据`、`旧 B42 参考` 或 `B41 弱参考`，并区分 `代码确认`、`作者声明`、`合理推断`、`待实机验证`。
8. 参考 MOD 只借鉴接口和实现模式，不直接复制受版权保护的大段代码、贴图、模型、音效。
9. 每次发现新的坑、接口、测试结论、版本决策，都必须沉淀回 `GodSystem_DevHandoff_CN`，防止后续会话遗忘。
10. 当前实时目录已使用 Git 管理，以提交和稳定版本标签作为主要开发历史；不再要求每次升级都生成滚动 ZIP。只有用户明确要求或进行高风险目录迁移时，才运行 `Backup-GodSystem.ps1`。
11. 所有开发先在 Git worktree 完成并验证，再覆盖 Workshop 运行文件让用户直接进游戏测试；用户确认后更新实测记录并推送 GitHub。稳定备份 zip 只有用户明确要求时才处理。
12. 每次改动 Lua、翻译、脚本、说明文本后，必须运行仓库内 `tools\Test-GodSystem.ps1`。不要用 PowerShell 控制台显示效果判断中文文件是否真实乱码。

## 当前关键结论

- v1.16.53 属性枚举使用 `Perks.getMaxIndex()` / `Perks.fromIndex()` 找技能枚举值，但名称、父类和累计等级 XP 优先从 `PerkFactory.getPerk(perk)` 返回的注册技能对象读取。MP 由服务端重新报价、按活期后现金扣款，使用无倍率 `AddXP(perk, amount, false, false, false, false)` 后再调用 `SyncXp()`；失败按原资金来源退款。
- v1.16.53 整车修复沿用原版 B42.19 `vehicle:repair()`，通过 `vehicle:getId()` / `getVehicleById()` 定位。必须先验证模块、车辆、楼层和 4 米距离，先移除模块再修复复查，失败返还新模块。
- B42.19 自定义可穿戴容器需要完整四步：注册命名空间位置、加入 Human BodyLocationGroup、脚本 `BodyLocation`、脚本 `CanBeEquipped`。v1.16.54 漏掉 Human Group 注册，不能作为完整方案失败证据；当前 v1.16.63 使用 `GodSystem:SystemSpaceTerminal`，并已通过本轮用户测试。
- B42.19 直接对 GodSystem 终端调用 `setCapacity()` 的安全值仍为49。v1.16.62 的 Lua 方法包装无法覆盖 Java 内部容量检查，已在 v1.16.63 删除；额外空间改由终端内隐藏 Food 实例的原生负重量提供。
- v1.16.63 已由用户确认发布，是下一版本必须维护的兼容基线。以后仍只有用户明确确认发布的版本才建立兼容负担；未发布测试版本默认可以删除过时物品、字段和迁移逻辑。
- `GodSystem_Server.lua` 已接近 Lua 5.1 单 chunk 的 200 个顶层 local 上限。新增服务器 helper 应优先挂到 `GodSystemServer` 命名空间或拆分模块；每版必须对全部 Lua 执行 `C:\Users\Admin\Tools\Lua51\luac.exe -p`。
- v1.16.48 隐藏旧回收页但保留底层兼容代码；新交易入口是原版库存右键。SP/MP 都必须按 `item:getID()` 精确定位，外部容器物品先通过原版转移动作进入玩家库存，删除后确认物品确实离开容器，最后才发币或写上架状态。
- v1.16.48 多人理财不再依赖服务端 `OnPlayerUpdate`。客户端只累计本次在线会话的世界小时并发送正整数小时，服务端累计到 24 小时后结算；断线重进不补离线时间。
- v1.16.48 的 64 项沙盒配置只在 SP 角色或 MP 服务端还没有持久 `settings` 时导入。进入世界后仍以系统管理页为运行时权威，旧存档绝不能被沙盒值覆盖。
- v1.16.46 新增自动存款、自动交任务和三个风险理财账户。SP 本地结算，MP 资金/物品/理财由服务端结算；自动操作顺序固定为腰包回收、任务领取、现金存款。理财只累计在线游戏时间，首次日结前不可赎回，死亡不扣理财余额。
- v1.16.42 当前修复：治愈病毒在清 BodyDamage、感染时间、死亡时间和身体部位感染之外，还会清 `player:getStats():set(CharacterStat.ZOMBIE_INFECTION, 0)`；该逻辑参考同版本 RaccoonCityB42 的 `Base.BiochemicalVaccines` 生化疫苗。
- v1.16.44 修复：银行借贷 UI 乱码来自 v1.16.43 新增文本的本地化层问题。修复点是补齐 `Translate\CN/CH\IG_UI_*.txt` 借贷表项行尾逗号，并把 `GodSystem_Localization_Override.lua` 中借贷与管理员借贷配置 fallback 改为 ASCII-only 的 UTF-8 字节转义。新增专项测试 `Test-GodSystemV11644.ps1`。
- v1.16.43 已实现银行借贷系统，见 `32_v1.16.43_银行借贷系统技术方案.md`、`33_v1.16.43_银行借贷系统实施计划.md`、`34_v1.16.43_银行借贷系统实现.md`。已确认并实现：首版单笔贷款、还款只扣活期、信用破产不清死期、玩家死亡不清空欠款；破产用 `loanCreditSpentOffset` 重置成长额度，不直接重置 `data.stats.spentPoints`。
- v1.16.41 修复：医疗服务中“压制病毒”已废弃并从 UI、配置、SP/MP 服务分支和本地化移除。不要再使用 `setInfectionTime(0)` 这类压制方案；用户实测会瞬间暴毙。
- v1.16.40 结论保留：治愈病毒价格 2000，需要同时清 `BodyDamage:setInfected(false)`、`setIsFakeInfected(false)`、感染进度、死亡时长，以及每个 body part 的 `SetInfected(false)` / `SetFakeInfected(false)` / `setInfectedWound(false)` / `setWoundInfectionLevel(-1)`。
- v1.16.39 上一版新增：回收页右键“仅上架”，费用为商城购买价 50% 且最低 50，只解锁商城购买，不删除物品；多人由服务端验证真实物品和余额。
- v1.16.36 是快捷动作栏基线：快捷窗不再打开页面，只执行回家、返回、卖腰包、存现金；左侧导航有上翻/下翻按钮，不依赖滚轮发现后续功能。
- v1.16.35 是上一版测试基线：升级页新增感染检查、压制感染、治疗伤势、清除病毒；多人通过服务端 `medicalService` 结算；标题栏新增基础快捷窗；左侧导航底部按钮越界已修。
- v1.16.34 是上一版基线：抽奖奖池过滤隐藏/废弃/debug/伤口/僵尸伤害等不应发奖物品。
- v1.16.26：主窗口可从右下角拖拽等比缩放，保持 1240x690 设计比例；最小 75%，最大按主题和当前屏幕限制；缩放比例保存到 `data.ui.windowScale`。任务追踪窗口和弹窗暂不纳入这一版主窗口缩放。
- v1.15.3 是用户当前确认成功的正确版本。
- v1.15.6 是当前单人功能稳定基线，包含腰包自动回收、任务追踪窗口缩放、传送移动误判修复、删除出发点、主系统 UI 缩放自适应。
- v1.16.13 的银行手动钱币整理、多人服务端整理、多人腰包自动回收客户端计时触发等功能保留在 v1.16.16 中。
- `that DAMN Library` 不是必须依赖；它只是通过 fallback 修复了缺失 `ItemType` 的物品脚本问题。
- GodSystem 自身已修复系统币脚本：`SystemCoin1/10/100` 使用 `ItemType = base:normal`，并补了 `WorldStaticModel = Money`。
- 用户已测试：只启用 GodSystem 一个 MOD 可以正常进游戏。
- 当前稳定 zip 已覆盖为上述测试成功版本，不包含 `damnlib` 或 `RuinBazaar`。

## 不要再误判

- v1.16.14 是失败废弃版本：用户测试表现为只显示少数图标、不显示文字、红字连续报错。不要再以 v1.16.14 实现为基线。
- v1.16.16 基于 v1.16.13/v1.16.15 稳定逻辑重制 UI，避免 `drawText(..., gsTextColor(...), UIFont.Small)` 这类 Lua 多返回值参数错位；固定大面板仍不复用失败的 v1.16.14 实现。
- 不要把 v1.15.4 当当前稳定版。
- 每次修改功能都要同步迭代 `GodSystemConfig.Version`、两个 `mod.info` 的 `modversion`，并写入 `07_版本记录.md`。
- Git 提交是当前主要版本记录；新功能先建分支，验证后提交并合并。已有滚动备份保留但不强制继续生成，也不能用 Git 或滚动备份替代用户确认过的稳定发布包。
- 不要把 `damnlib` 打包进 GodSystem；它的许可不允许随意 repack，而且现在也不需要。
- 不要只改 `Translate\CN`；GodSystem 运行时优先读 `GodSystem_Localization_Override.lua` 里的 fallback。
- 不要直接凭记忆改 PZ B42 脚本字段；先看同版本官方手册/原版文件，再看同版本参考 MOD 的写法。
- 不要把 PowerShell 里看到的中文乱码直接当成文件损坏；先用严格 UTF-8 读取或运行 `Test-GodSystemEncoding.ps1` 判断真实编码。

## v1.16.68 当前接入入口

- 共享模型与有界索引：`GodSystem_Storage.lua`。
- 网络持久化、权限、旧连接迁移、物理拓扑、路由和单件恢复事务：`GodSystem_StorageManager.lua`。
- MP 独立协议与服务端命令：`GodSystem_StorageServer.lua`；主服务文件只增加一个 `require`，没有继续堆顶层 local。
- SP/MP 客户端桥接：`GodSystem_StorageClient.lua`。
- 独立仓储/容器管理窗口：`GodSystem_StorageUI.lua`。
- `GodSystem_StorageContext.lua` 只负责控制器打开/收回、连接模式、家具级标记和轻量符号绘制；控制器落地走原版普通物品“放置物品”，没有自制安装光标。
- 控制器物品仍为 `GodSystem.StorageController`，不是容器；使用 `base:normal` 与 `CarBatteryCharger` 静态世界模型，不再借用 `recreational_01_16` 街机柜类型或名称。网络身份同时保存在物品和世界对象 ModData。
- SP/MP 客户端控制器参数同时携带 `x/y/z` 与 `controllerX/Y/Z`；共享管理器兼容两套字段。禁止再次让 SP 直接调用与 MP 服务端归一化使用不同参数。
- 使用说明由主 UI 的 `storageInfo` 详情分支显示；仓储和容器管理列表只能恢复为确定的数字行号，不能读取首次 `prerender()` 才写入的 `item.index`。
- 连接模式只在状态变化、手动刷新或标记操作后扫描并缓存坐标；`OnPreUIDraw` 只绘制缓存的小菱形符号，不得恢复 `setHighlighted(true)` 或逐帧扫描场景对象。
- 主系统页通过 `controllerStatus` 区分未领取、安装包、已安装和遗失；首次免费，后续找回固定2000系统币。领取、找回和家具标记均使用独立操作ID。
- 当前仍需实机核对：普通世界物品的 SP/MP 放置—保存—拾取往返、冰箱/冰柜多容器索引、普通对象与 IsoThumpable 的标记生命周期，以及两人并发存取。

## v1.16.63 兼容基线入口

- 终端容量、减重和三线事务门面在 `GodSystem_TerminalUpgrades.lua`；空间减载公式、隐藏物品审计、快照和逃逸清理在 `GodSystem_TerminalRelief.lua`。`GodSystem_TerminalCapacity.lua` 已删除。
- 旧压缩只由 `GodSystem_LegacyCompressionCleanup.lua` 做一次性恢复，禁止恢复主动压缩路线、共享 `ScriptItem` 改重或容器变化扫描。
- 终端容量固定 Lv.1-8：10/15/20/25/30/35/42/49；减重仍为独立 Lv.1-8。减载默认每次2000币、每级5点、最多2000点，等级字段为 `autoRecyclerReliefLevel`。
- `TerminalReliefUpgradeCost`、`TerminalReliefPerLevel`、`TerminalReliefMaxOffset` 已进入71项管理/沙盒配置；沙盒只初始化新世界，系统管理页是运行时权威。
- 隐藏内部物品 `GodSystem.SystemTerminalRelief` 必须保持恰好一个顶层实例，并设置 `Hidden`、`favorite`、`unwanted`；回收、自动上架、抽奖和玩家可见数量均排除它。审计只在领取/找回、终端查找、页面打开、升级、自动回收、SP启动、MP握手/刷新等明确边界执行。
- 商品可见性仍用 `setShopItemHidden`；真正删除只允许从隐藏管理弹窗确认后调用 `deleteShopItem`。删除不退款，旧 `removeUnlocked` 永远保持隐藏兼容语义。

## v1.16.61 接入基线

- 商城身份、旧记录归一化、家具变体创建与贴图核验集中在 `GodSystem_ShopVariants.lua`；普通物品键为 `fullType`，家具键为 `fullType + worldSprite`，商城配置入口继续使用稳定 `id`。
- v1.16.61 当时玩家上架记录只通过 `hidden` 切换可见性；v1.16.62 已在隐藏管理中增加明确确认的删除上架操作。隐藏的 SP/MP 接口与旧命令兼容语义不变。
- 商城隐藏管理弹窗在 `GodSystem_UI.lua`，只遍历已同步的 `unlockedShopItems`；分类、状态和搜索不会扫描背包，也不会发起筛选网络请求。隐藏商品不进入商城主页/分类/搜索，但 `lotteryCandidates()` 仍读取全部玩家上架记录。
- 单件上架由 UI 传入真实 `itemId`，SP/MP 重新确认物品身份；内置商品、正常上架商品和隐藏商品都会跳过且不收费，隐藏商品提示从管理窗口恢复。家具购买在扣币前创建并复核 `worldSprite`，失败移除本次发放物。

- 永久负重共享计算与幂等校准在 `GodSystem_CarryCapacity.lua`；SP 购买和生命周期接入在 `GodSystem_Core.lua`，MP 权威购买/刷新在 `GodSystem_Server.lua`，升级行和专用刷新按钮在 `GodSystem_UI.lua`。
- `data.upgrades.carryCapacityLevel` 是永久等级字段，旧存档缺失时归一为 0。GodSystem 不写 `maxWeightBase`；每次校准先扣除自己上次倍率，再以 `等级×2 / getMaxWeightBase()` 换算原生增量，最后核验外部基础加成后的最终值。
- 右键智能回收预筛和外部容器转移在 `GodSystem_RecycleContext.lua`；SP 二次验证在 `GodSystem_Core.lua`，MP 服务端二次验证、删除回滚和真实经济结算在 `GodSystem_Server.lua`。
- `GodSystem_TransactionOps.lua` 保存全部 `upgradeSystem` 类型与 `recycleSelectedItems` 的操作结果；客户端超时重试只在请求指纹一致时复用原操作 ID。不要把客户端预筛数量用于服务端金额计算。
- 终端减重必须同时校准终端物品外层和内容容器内层；空间减载只修改内部 Food 实例的 `hungChange`，绝不修改普通物品或共享 `ScriptItem`。
- 最近一次终端校准的实际容量、减重和减载由 `getAppliedStatus()` 提供；玩家可见物品重量会剔除隐藏减载贡献，并显示约等于“原生容量+当前减载”的等效空间。
- v1.16.63 减载专项入口为 `tools/tests/Test-GodSystemV11663.ps1`，多人穿戴、即时同步和车辆修复专项分别为 `Test-GodSystemV11663TerminalWear.ps1`、`Test-GodSystemV11663TerminalSync.ps1` 和 `Test-GodSystemV11663VehicleRepair.ps1`。统一入口已通过编码检查、29 个 Lua 的 Lua 5.1 编译和 Workshop 哈希校验；用户已确认测试通过并发布到创意工坊。
- 人物负重仍以 `maxWeightDelta` 保存 GodSystem 自身倍率，不修改 `maxWeightBase`；应用时使用 `setMaxWeight()` 刷新当前整数最终值，避免旧缓存导致同步核验失败。
- `GodSystem_CarryCapacity.getStatus()` 的 `actualBonus` 只由当前最终负重减去已记录基础负重得到；`predictedIncrease/predictedFinal` 只在现有应用调用中测算，不挂事件。升级页不得恢复为读取理论 `status.bonus`，也不得把负重计算放入 `prerender` 或 `OnPlayerUpdate`。
- 压缩元数据版本为 2，新增基础/最后 setter 输入字段。`getWeight()` 与脚本定义只允许读取，禁止对共享定义调用 `setActualWeight()`、`setWeight()` 或 `DoParam()`。

- 空间终端主类型与 Lv.1-8 数值在 `GodSystem_Config.lua` / `GodSystem_Items.txt`；穿戴位置为 `GodSystem:SystemSpaceTerminal`，由 `media/registries.lua` 和 `GodSystem_BodyLocations.lua` 完整注册。
- 属性共享报价在 `GodSystem_Attributes.lua`，SP 结算在 `GodSystem_Core.lua`，MP handler 为 `Commands.attribute`，页面在 `GodSystem_UI.lua`。
- 车辆右键入口在 `GodSystem_VehicleRepairContext.lua`，复用 `useMaintenanceItem`；共享损坏统计和修复同步在 `GodSystem_Maintenance.lua`，SP 桥接在 `GodSystem_SPVehicleRepair.lua`，MP handler 在 `GodSystem_Server.lua`。
- B42.19 `ISModalDialog` 回调参数为 `target, button, param1, param2`，构造函数中的 `player` 不占回调参数位；车辆确认 payload 作为 `param1` 传入，因此签名必须是 `Context.onConfirm(target, button, payload)`。
- 同伴五项特效数值、顺序和共鸣门槛在 `GodSystem_CompanionConfig.lua`；移动、蓄能、目标复验和统一伤害结算在 `GodSystem_Companion.lua`；像素矩阵编译、八方向覆盖层和固定粒子池在 `GodSystem_CompanionVisual.lua`。多人不加载同伴运行时。
- v1.16.56 机器人继续只使用 `renderline()` 和缓存的白色线纹理，不依赖机器人 PNG，也不修改全局纹理过滤。`GodSystem_CompanionVisual.lua` 加载时把 `32×24` 字符矩阵编译成横向线段，运行时只做缩放缓存、状态着色和固定池粒子更新。
- v1.16.46 是自动银行、自动交任务和风险理财的功能基线，不改变现有买卖、贷款、任务进度和腰包规则。
- v1.16.45 是腰包回收模式与中文生成流程基线。
- v1.16.43 是银行借贷系统功能基线：基于 v1.16.42，新增银行借贷系统。
- v1.16.32 是失败废弃的 UI 性能缓存路线，不要作为当前功能基线。
- 不要用 v1.16.14 作为 UI 或功能基线；后续新改先参照 v1.16.13、v1.16.15、v1.16.23、v1.16.29-v1.16.31、v1.16.33-v1.16.36 这类已实测或当前待测的稳定路径。
- v1.16.43 已实现，方案和实现记录在 `32_v1.16.43_银行借贷系统技术方案.md`、`33_v1.16.43_银行借贷系统实施计划.md`、`34_v1.16.43_银行借贷系统实现.md`；后续继续开发前仍要先备份当前实时目录。
- v1.16.44 的乱码修复记录在 `35_v1.16.44_银行借贷本地化乱码修复.md`；后续新增 Lua fallback 中文时必须照此处理。
